/* Global runtime-configurable variables for topology-master-vc3 */
#include "route_sim_anytopo.h"

/* default runtime VC count (can be overridden via topology argv) */
int ROUTE_SIM_ANYTOPO_VC_RUNTIME = ROUTE_SIM_ANYTOPO_VC;
